---

- tags: [ bootstrap ]
  become: true
  block:

  - debug:
      msg: "using the generic boot strapper, this is probably not enough"

  - name: install shim packages for Linuix (generic)
    package:
      name:
        - python-pip
        - unzip
        - git
